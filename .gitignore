# secrets & personal data — never commit these
settings.json
gemini.key
# android signing: the keystore and the passwords that open it
*.jks
android/keystore.properties
android/local.properties
notes/
shots/
debug.log
mike-actions.log

# python runtime & build outputs
venv/
__pycache__/
build/
dist/
*.spec

# packaged app (rebuilt from source; too big for git)
_internal/
DictationMic.exe
DictationMic-Android.apk
DictationMic-Wear.apk
models/
selftest_out.txt
.gradle/
.kotlin/

# deploy staging — regenerated with: robocopy web hosting /MIR /XD downloads
# (hosting/downloads holds the 110MB friend zip, over GitHub's file limit)
hosting/
.firebase/
